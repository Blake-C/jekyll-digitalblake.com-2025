---
layout: null
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/feed.xml' | absolute_url | xml_escape }}</id>

  {% assign feed_title = site.title | default: site.name %}
  {% if feed_title %}
  <title type="html">{{ feed_title | xml_escape }}</title>
  {% endif %}

  {% if site.description %}
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% endif %}

  {% if site.default_og_image %}
  <logo>{{ site.default_og_image | absolute_url | xml_escape }}</logo>
  {% endif %}
  <icon>{{ '/assets/icons/favicon-32x32.png' | absolute_url | xml_escape }}</icon>

  {% if site.author %}
  <author>
    <name>{{ site.author.name | default: site.author | xml_escape }}</name>
    {% if site.author.email %}
    <email>{{ site.author.email | xml_escape }}</email>
    {% endif %}
    {% if site.author.uri %}
    <uri>{{ site.author.uri | xml_escape }}</uri>
    {% endif %}
  </author>
  {% endif %}

  {% assign posts = site.posts %}
  {% assign posts = posts | where_exp: "post", "post.draft != true" %}
  {% assign posts = posts | sort: "date" | reverse %}
  {% assign posts_limit = site.feed.posts_limit | default: 10 %}
  {% for post in posts limit: posts_limit %}
  <entry>
    {% assign post_title = post.title | smartify | strip_html | normalize_whitespace | xml_escape %}
    <title type="html">{{ post_title }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post_title }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.modified_date | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url | xml_escape }}</id>
    <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}"><![CDATA[{{ post.content | strip }}]]></content>

    {% assign post_author_id = post.author | default: site.author.id %}
    {% assign post_author = site.data.authors[post_author_id] %}
    <author>
      <name>{{ post_author.name | default: site.author.name | xml_escape }}</name>
      {% assign author_email = post_author.email | default: site.author.email %}
      {% if author_email %}
      <email>{{ author_email | xml_escape }}</email>
      {% endif %}
      {% assign author_uri = post_author.uri | default: site.author.uri %}
      {% if author_uri %}
      <uri>{{ author_uri | xml_escape }}</uri>
      {% endif %}
    </author>

    {% for category in post.categories %}
    <category term="{{ category | xml_escape }}" />
    {% endfor %}
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}" />
    {% endfor %}

    {% assign post_summary = post.description | default: post.excerpt %}
    {% if post_summary and post_summary != empty %}
    <summary type="html"><![CDATA[{{ post_summary | strip_html | normalize_whitespace }}]]></summary>
    {% endif %}

    {% assign post_image = post.image.path | default: post.image | default: site.default_og_image %}
    {% if post_image %}
      {% unless post_image contains "://" %}
        {% assign post_image = post_image | absolute_url %}
      {% endunless %}
    <media:thumbnail url="{{ post_image | xml_escape }}" />
    <media:content medium="image" url="{{ post_image | xml_escape }}" />
    {% endif %}
  </entry>
  {% endfor %}
</feed>
